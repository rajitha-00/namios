export interface BentoItemProps {
  className?: string;
  children?: React.ReactNode;
}

export type SupportedLanguage = "en" | "si" | "ta";

export type PortalRole = "owner" | "reception" | "housekeeping" | "fb";
